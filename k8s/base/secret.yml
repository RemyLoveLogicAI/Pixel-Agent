apiVersion: v1
kind: Secret
metadata:
  name: api-server-secrets
  namespace: pixel-agent
  labels:
    app.kubernetes.io/name: api-server
    app.kubernetes.io/part-of: pixel-agent
type: Opaque
stringData:
  # Replace with real values before applying — never commit actual secrets
  DATABASE_URL: "postgres://pixel:pixel@postgres:5432/pixel_agent"
